spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/763678968/Trend-Trading/
          searchPaths: trendConfig/respo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/